{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Pipali",
            "type": "bun",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/src/server/index.ts",
        }
    ]
}